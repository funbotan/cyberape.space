name: Deploy via Pelican on GitHub pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  pelican:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'true'
    - uses: rehanhaider/pelican-to-github-pages@v1.3.0
      env:
        GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        GH_PAGES_CNAME: cyberape.space